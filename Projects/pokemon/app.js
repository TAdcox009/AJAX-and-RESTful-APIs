// <imh src="..." class="car-img-top" alt="...">

// Arceus
var _condition = "pokemon"
var _pocketmonster = "arceus"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#arceus');
    pokeIMG = `<img src="${img.front_default}" id="arceus">`;
    selector.innerHTML = pokeIMG;
}
    );

    // pikachu
var _condition = "pokemon"
var _pocketmonster = "pikachu"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#pikachu');
    pokeIMG = `<img src="${img.front_default}" id="pikachu">`;
    selector.innerHTML = pokeIMG;
}
    );

     // mew
var _condition = "pokemon"
var _pocketmonster = "mew"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#mew');
    pokeIMG = `<img src="${img.front_default}" id="mew">`;
    selector.innerHTML = pokeIMG;
}
    );

     // eevee
var _condition = "pokemon"
var _pocketmonster = "eevee"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#eevee');
    pokeIMG = `<img src="${img.front_default}" id="eevee">`;
    selector.innerHTML = pokeIMG;
}
    );

     // snorlax
var _condition = "pokemon"
var _pocketmonster = "snorlax"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#snorlax');
    pokeIMG = `<img src="${img.front_default}" id="snorlax">`;
    selector.innerHTML = pokeIMG;
}
    );

     // ditto
var _condition = "pokemon"
var _pocketmonster = "ditto"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#ditto');
    pokeIMG = `<img src="${img.front_default}" id="ditto">`;
    selector.innerHTML = pokeIMG;
}
    );

    // charizard
var _condition = "pokemon"
var _pocketmonster = "charizard"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#charizard');
    pokeIMG = `<img src="${img.front_default}" id="charizard">`;
    selector.innerHTML = pokeIMG;
}
    );

     // celebi
var _condition = "pokemon"
var _pocketmonster = "celebi"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#celebi');
    pokeIMG = `<img src="${img.front_default}" id="celebi">`;
    selector.innerHTML = pokeIMG;
}
    );

     // gengar
var _condition = "pokemon"
var _pocketmonster = "gengar"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#gengar');
    pokeIMG = `<img src="${img.front_default}" id="gengar">`;
    selector.innerHTML = pokeIMG;
}
    );

    // gyarados
var _condition = "pokemon"
var _pocketmonster = "gyarados"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#gyarados');
    pokeIMG = `<img src="${img.front_default}" id="gyarados">`;
    selector.innerHTML = pokeIMG;
}
    );

     // raichu
var _condition = "pokemon"
var _pocketmonster = "raichu"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#raichu');
    pokeIMG = `<img src="${img.front_default}" id="raichu">`;
    selector.innerHTML = pokeIMG;
}
    );

     // lopunny
var _condition = "pokemon"
var _pocketmonster = "lopunny"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#lopunny');
    pokeIMG = `<img src="${img.front_default}" id="lopunny">`;
    selector.innerHTML = pokeIMG;
}
    );

     // pichu
var _condition = "pokemon"
var _pocketmonster = "pichu"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#pichu');
    pokeIMG = `<img src="${img.front_default}" id="pichu">`;
    selector.innerHTML = pokeIMG;
}
    );

     // jynx
var _condition = "pokemon"
var _pocketmonster = "jynx"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#jynx');
    pokeIMG = `<img src="${img.front_default}" id="jynx">`;
    selector.innerHTML = pokeIMG;
}
    );

     // gible
var _condition = "pokemon"
var _pocketmonster = "gible"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#gible');
    pokeIMG = `<img src="${img.front_default}" id="gible">`;
    selector.innerHTML = pokeIMG;
}
    );

     // psyduck
var _condition = "pokemon"
var _pocketmonster = "psyduck"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#psyduck');
    pokeIMG = `<img src="${img.front_default}" id="psyduck">`;
    selector.innerHTML = pokeIMG;
}
    );

     // leafeon
var _condition = "pokemon"
var _pocketmonster = "leafeon"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#leafeon');
    pokeIMG = `<img src="${img.front_default}" id="leafeon">`;
    selector.innerHTML = pokeIMG;
}
    );

     // gyarados
var _condition = "pokemon"
var _pocketmonster = "gyarados"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#gyarados');
    pokeIMG = `<img src="${img.front_default}" id="gyarados">`;
    selector.innerHTML = pokeIMG;
}
    );

     // jigglypuff
var _condition = "pokemon"
var _pocketmonster = "jigglypuff"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#jigglypuff');
    pokeIMG = `<img src="${img.front_default}" id="jigglypuff">`;
    selector.innerHTML = pokeIMG;
}
    );

     // piplup
var _condition = "pokemon"
var _pocketmonster = "piplup"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#piplup');
    pokeIMG = `<img src="${img.front_default}" id="piplup">`;
    selector.innerHTML = pokeIMG;
}
    );

     // lapras
var _condition = "pokemon"
var _pocketmonster = "lapras"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#lapras');
    pokeIMG = `<img src="${img.front_default}" id="lapras">`;
    selector.innerHTML = pokeIMG;
}
    );

    // ninetales
var _condition = "pokemon"
var _pocketmonster = "ninetales"

fetch(`https://pokeapi.co/api/v2/${_condition}/${_pocketmonster}`)
.then( res => res.json())
.then(data => {
    console.log(data.sprites)
    const {...img} = data.sprites;
    // console.log(img)
    selector = document.querySelector('#ninetales');
    pokeIMG = `<img src="${img.front_default}" id="ninetales">`;
    selector.innerHTML = pokeIMG;
}
    );
